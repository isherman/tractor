name: Publish Source to GH Releases

# For all releases (tags beginning with 'v'), publish source archives for all modules.
on:
  push:
    tags:
      - "v*.*.*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: ["core"]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: "recursive"

      - name: Build project
        run: |
          tar -czvf ${{ matrix.image }}.tar.gz modules/${{ matrix.image }}

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./${{ matrix.image }}.tar.gz
          asset_name: ${{ matrix.image }}.tar.gz
          asset_content_type: application/gzip
