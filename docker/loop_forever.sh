#!/bin/bash
echo "farm-ng's devel container is running.  Press Ctrl-C to stop it."
echo "You can open up a shell inside the container:"
echo "  docker exec -it docker_devel_1 bash"
while true
do
    sleep 1
done
